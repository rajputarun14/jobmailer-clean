package com.arun.jobmailer.rateLimiter.service;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;

class RateLimiterServiceTest {

   /*  @Test
    void limitsPerUserIndependently() {
        RateLimiterService service = new RateLimiterService(1, 1);

        assertThat(service.allowRequest("alice")).isTrue();
        assertThat(service.allowRequest("alice")).isFalse();
        assertThat(service.allowRequest("bob")).isTrue();
    }*/
}
